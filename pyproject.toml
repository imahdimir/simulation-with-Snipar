[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "snipar-simulate-save-last-3-gens"
version = "0.0.0"
authors = [{ name = "Mahdi Mir", email = "imahdimir@gmail.com" }]
readme = "README.md"
dependencies = [
    "snipar==0.0.18,
]
